
const STREAM_API_KEY =  process.env.EXPO_PUBLIC_STREAM_API_KEY

const syncUserToStream = 


const Chatclient = () => {
    return null;
}

const ChatWrapper = ({children}:{children: React.ReactNode}) => {
  return 
    <ChatClient>
        {children} 
    </ChatClient>
}

export default ChatWrapper